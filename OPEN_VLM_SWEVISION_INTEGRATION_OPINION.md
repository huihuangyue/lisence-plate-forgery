# 本地开源视觉模型选型与直接调用 SWE‑Vision 工具意见书

- 日期：2026-08-25
- 状态：技术选型意见，不包含代码改造
- 适用环境：Ubuntu 22.04 / WSL2、RTX 4060 Ti 8GB、约 16GB 主存；未来可迁移至 A6000
- 目标：用本地开源模型替代不稳定且成本较高的云端视觉调用，并判断是否应直接调用仓库内 SWE‑Vision 的现成工具代码增强车牌贴片检测

## 一、结论

当前约束下，建议选择 **`Qwen/Qwen3.5-2B` 官方 BF16 权重**作为唯一生产候选模型，并用本项目现有 100 张人工标注集完成准入测试。

这不是因为必须选 Qwen，而是因为本任务同时需要：

1. 读取矫正车牌、字符槽位图、诊断面板等多张图片；
2. 识别细小文字和局部材料异常；
3. 按固定 JSON 协议输出字符槽位；
4. 在需要时可靠地产生函数调用，并为 SWE‑Vision 的 `execute_code` 编写可执行 Python；
5. 在 8GB 显存上运行，并允许以后做小规模领域微调；
6. 通过 OpenAI 兼容接口替换当前云端端点，减少主流水线改动。

`Qwen3.5-2B` 是目前找到的候选中对这六项约束最均衡的一个。它不是对贴牌任务“已经证明准确率最高”的模型；公开榜单不存在同色贴片、白边暗缝、翘边和车牌纵向渐变异常这一专门任务。最终优劣必须由本项目独立标注集决定。

对于 SWE‑Vision，建议的直接复用方案是：

- **直接导入并调用 `VLMToolCallAgent.run()`，但只作为疑难样本二次复核器，不替换全部生产路径。** 这会真实启用其 `execute_code` 工具、持久化 Jupyter kernel、工具结果图像回传和轨迹记录。
- 不建议每张图都运行原始 SWE‑Vision 智能体。它会增加模型轮次、CPU 代码执行和容器管理开销，而且现有六图试验没有真值，不能证明其自动检测准确率更高。
- `JupyterNotebookKernel.execute()`、`NotebookFileManager` 和 `TrajectoryRecorder` 都能单独直接调用，但单独调用它们不会让模型自主选择分析方法；只有完整的 `VLMToolCallAgent` 才包含“模型决定何时执行什么代码”的智能体循环。

## 二、模型证据与排序

### 2.1 主选：Qwen3.5-2B

官方模型卡给出的、与本项目较相关的指标如下。斜杠两侧为官方列出的 thinking / non-thinking 两种结果；这些都是通用代理指标，不是贴牌准确率：

- OCRBench：`84.5 / 85.4`；旧的 Qwen3-VL-2B 为 `79.2`。
- CC-OCR：`72.9 / 75.8`；旧的 Qwen3-VL-2B 为 `68.3`。
- RealWorldQA：`74.5 / 71.2`；旧的 Qwen3-VL-2B 为 `69.5`。
- HallusionBench：`58.0 / 51.3`；旧的 Qwen3-VL-2B 为 `54.9`。
- RefCOCO 平均：`84.8 / 84.3`，说明其具备较好的视觉定位基础。
- BFCL-V4 工具调用指标：`43.6`。绝对值并不高，但在本次核查的轻量候选中，它同时提供了最明确的官方工具调用部署方式和这一类公开指标。

官方权重目录约 `4.57GB`，许可为 Apache-2.0。官方文档明确提供 vLLM、SGLang 和 OpenAI 兼容接口，并为工具调用给出 `qwen3_coder` parser；也给出 MTP 推理加速方式。来源：[Qwen3.5-2B 官方模型卡](https://huggingface.co/Qwen/Qwen3.5-2B)、[官方权重目录](https://huggingface.co/Qwen/Qwen3.5-2B/tree/main)。

选择它而不是 Qwen3-VL-2B 的直接理由是：同一官方模型卡中的 OCR、真实场景、文档理解和智能体指标整体更强，而且它是面向原生多模态智能体的新一代模型。选择 2B 而不是 4B 的理由是：4B 官方 BF16 仓库约 `9.34GB`，仅权重就超过当前 8GB 显存，尚未计入 KV cache、视觉张量和 CUDA 工作区；第三方 4bit 量化虽可能装下，但量化后的细粒度视觉和工具调用可靠性没有本项目证据，不应直接作为首个生产基线。来源：[Qwen3.5-4B 官方权重目录](https://huggingface.co/Qwen/Qwen3.5-4B/tree/main)。

部署时应先采用 BF16、短上下文和单并发，不应一开始启用 MTP。MTP 的确有官方支持，但它优化的是解码吞吐，不会自动提高贴牌准确率；待准确率基线稳定后再单独做速度消融。

这里仍有一个必须实测的不确定项：官方权重约 4.57GB，不等于三张高分辨率图片、KV cache 和 vLLM 运行时的峰值显存。8GB 显存是否稳定必须用项目真实三图请求验证，并限制上下文和并发；官方当前还要求较新的 vLLM/SGLang 版本，这也增加了部署版本风险。

### 2.2 第二名：MiniCPM-V 4.6 Instruct

MiniCPM-V 4.6 总参数约 1.3B，官方给出的 GPU 内存需求约 4GB，支持单图、多图、vLLM、SGLang、llama.cpp 和 Ollama。官方声称其在 OCRBench、RefCOCO、HallusionBench、MUIRBench 等任务达到 Qwen3.5-2B 级别，并通过 4x/16x 视觉 token 压缩降低视觉计算量。来源：[OpenBMB 官方仓库](https://github.com/OpenBMB/MiniCPM-V)、[官方模型卡](https://huggingface.co/openbmb/MiniCPM-V-4.6)、[官方 vLLM 指南](https://github.com/OpenSQZ/MiniCPM-V-CookBook/blob/main/deployment/vllm/minicpm-v4_6_vllm.md)。

如果只保留当前“预先生成三张证据图，再让模型输出 JSON”的静态路径，它可能是更快、更省显存的候选。但当前意见同时考虑直接运行 SWE‑Vision 的代码工具。MiniCPM-V 4.6 的公开资料没有给出可与 Qwen BFCL 对照的工具调用成绩，也没有提供同等明确的 `execute_code` 函数调用 parser。因此它适合作为速度备选，不作为当前唯一主选。

### 2.3 第三名：InternVL3.5-2B

InternVL3.5-2B 约 2.3B 参数，官方报告中的 OCRBench 为 `836/1000`，九项 OCR/文档指标平均 `76.7`，MuirBench 为 `44.0`，多图六项平均 `50.5`。其支持多图和 vLLM OpenAI 兼容服务。来源：[InternVL3.5 技术报告](https://arxiv.org/html/2508.18265)、[官方仓库](https://github.com/OpenGVLab/InternVL)、[vLLM 官方示例](https://github.com/vllm-project/recipes/blob/main/InternVL/InternVL3_5.md)。

它是合理的视觉备选，但在当前任务最关键的“本地小模型稳定函数调用并编写图像处理代码”方面，官方资料不如 Qwen3.5-2B 明确；动态高分辨率切图还会增加多图推理时的显存和时延不确定性。

### 2.4 未入选类别

- OCR 专用模型可能在文字转写上更强，但本任务的核心是材料边界、光影、表面连续性和字符槽位归属，不是纯文字识别。它们不能作为唯一裁决模型。
- 4B 及以上 BF16 模型在 A6000 上更有潜力，但当前服务器必须能独立跑完流程，因此不能作为这次主模型。
- 社区量化的 4B 模型可能比 2B 更强，也可能因量化损伤细小色差、结构化输出或工具调用；在没有同一数据集实测前，不把“参数更多”当成已成立的优势。

## 三、公开指标的适用边界

公开指标主要覆盖 OCR、文档、一般视觉问答、多图理解、视觉定位和函数调用。它们能够说明模型具有完成本任务的基础能力，但不能证明下列能力：

- 识别与车牌底色相近的矩形贴片；
- 区分贴片缝与冲压、反光、压缩、污渍；
- 判断未闭合但与车牌边框近似平行或垂直的接缝；
- 判断新能源车牌应有纵向渐变，而局部贴片颜色分布反向或过度均匀；
- 将异常稳定映射到正确字符槽，抑制相邻槽串扰。

因此，“最优”在本意见中指 **基于现有硬件、接口和工具调用要求的最优准入候选**，不是已经验证的任务最优模型。

建议使用以下准入指标，而不是只看 OCRBench：

- 整牌二分类：precision、recall、F1、accuracy、false-positive rate、false-negative rate。
- 字符槽级：micro/macro precision、recall、F1。
- 严格整牌：预测槽集合与人工槽集合完全一致的 exact-match accuracy。
- 可判别性：`unassessable` 率及其人工复核错误率。
- 工程指标：JSON 合法率、工具调用成功率、Python 执行成功率、P50/P95 时延、峰值显存、每图模型轮数。

## 四、SWE‑Vision 现成代码能否直接调用

结论是 **能直接调用，但不同入口的能力边界不同；完整智能体不是零适配替换件。**

### 4.1 `JupyterNotebookKernel.execute()`

位置：`SWEVISSION/swe_vision/kernel.py`。

可以直接导入 `JupyterNotebookKernel`，启动 Docker 内的持久化 Jupyter kernel，然后调用其公开异步方法 `execute(code)`。返回值已经结构化为：文本输出、base64 图片、状态和 traceback。

它直接提供：

- 隔离的 Python 运行环境；
- 同一 kernel 内变量和导入的持久化；
- matplotlib / IPython 图片捕获；
- 超时、文本截断和错误回传。

但它本身不是智能体：由生产程序写死代码再调用时，模型不会自主选择 ROI 或分析方法。它适合执行可信、固定的诊断脚本；若要模型自主生成代码，仍需上层 `VLMToolCallAgent`。

### 4.2 `VLMToolCallAgent.run()`

位置：`SWEVISSION/swe_vision/agent.py`。

这是唯一能够直接实现“模型看图 → 调用 `execute_code` → 查看数值或新图 → 再判断”的现成入口。它会：

1. 把多张输入图复制到 Docker 挂载目录；
2. 向 OpenAI 兼容端点发送 `execute_code` 和 `finish` 工具定义；
3. 执行模型生成的 Python；
4. 把文本结果和新生成图片作为工具消息送回模型；
5. 迭代直到模型调用 `finish`；
6. 保存完整轨迹。

因此，若目标是“直接调用 SWE‑Vision 的现有工具代码”，本意见推荐的就是这个入口，而不是重写一个相似工具。

仓库当前没有把 `SWEVISSION` 安装成项目顶层 Python 包的打包配置。真正导入前需要把 `<项目根>/SWEVISSION` 加入 `PYTHONPATH`，或补一个明确的可编辑安装步骤；否则 `from swe_vision import VLMToolCallAgent` 在普通项目环境中未必可见。这属于接线适配，不是重写工具。

但不能原样无条件替换当前 `StickerAgentHarness`，原因包括：

- 默认最多 `100` 次迭代，不适合逐图生产；必须在调用时限制为约 3–5 轮。
- 当前 `_call_llm()` 会附带面向特定服务商的 `reasoning` 扩展字段；本地 vLLM 是否接受必须先做接口测试。
- 当前方法未设置 `temperature=0`，与现有确定性流水线不同。
- `finish` 只返回字符串；还要经过当前项目的 JSON 提取、槽位白名单、字符映射、相邻槽串扰抑制和物理证据门控。
- 该类是异步接口，当前批处理是四线程同步执行，需要为每个 worker 固定 `kernel_slot` 并管理事件循环。
- 当前 kernel 端口从 `65500` 起、每个 slot 间隔 10；slot 0–3 有效，而 slot 4 会越过 TCP 端口上限。因此这份实现实际上最多安全映射 4 个并行 kernel，不能直接把 worker 数继续调高。
- kernel 在多次 `run()` 之间会保留 Python 状态；若一个 worker 连续处理多张图，必须防止上一张图的变量和文件污染下一张。
- 其 Docker 容器没有显式挂载 GPU，代码工具中的 OpenCV、NumPy、skimage 默认走 CPU；本地 VLM GPU 服务是另一个进程。
- `SWEVISSION/env/Dockerfile` 依赖一个外部 Volcengine 基础镜像，而 `requirements.txt` 没有固定 OpenCV、NumPy、scikit-image 等完整法证依赖，当前可复现性不足。

所以这里的“直接调用”是直接复用该类及其工具循环，而不是声称无需任何生产适配。

### 4.3 `NotebookFileManager`

位置：`SWEVISSION/swe_vision/file_manager.py`。

可以直接用于把当前六图或原始分辨率槽位图复制进 `/mnt/data`。它解决路径转换，不影响准确率。需要避免不同图片同名覆盖。

### 4.4 `TrajectoryRecorder`

位置：`SWEVISSION/swe_vision/trajectory.py`。

可以直接记录每次模型消息、工具参数、执行代码、输出图和最终答案。它对误报/漏报复盘很有价值，但会增加磁盘写入和图片存储，不直接提高识别率。当前生产代码已有自己的 `trajectory.json`，若启用应明确谁是唯一主轨迹，避免双份记录。

## 五、直接调用后能否提高效率或准确率

### 5.1 准确率：有潜力，但尚未被本项目证据证明

可能提高准确率的场景是：固定诊断面板不能覆盖所有局部异常，而模型可以临时裁剪某一字符、放大原始像素、比较同高度邻槽、改变色彩空间、画局部剖面或检查线段方向。

当前 `src/sticker/evidence.py` 已经计算了 CIELAB 色差残差、Sobel/Scharr 边缘、高频残差、亮暗边、轴对齐线、局部材料差异和新能源牌纵向渐变等证据。若 SWE‑Vision 只是让模型重新写一遍相同计算，准确率没有新增信息，效率还会下降。它真正可能带来的新增价值仅在于：针对当前样本自适应选择原分辨率 ROI、对照区、剖面方向和二次可视化。

外部证据表明工具增强对细粒度视觉可能有效：Qwen3-VL 技术报告称，在 V* 细粒度感知基准上，加入工具带来的提升约为 5 个点，并认为工具收益可超过单纯增加模型规模。这个结果只说明方向可行，不能外推成贴牌任务也会提升 5 点。来源：[Qwen3-VL 技术报告](https://arxiv.org/html/2511.21631)。

仓库外的 SWE‑Vision 车牌试验也不能作为准确率证明。该试验只有 6 张图、没有 ground truth，使用的是 `gpt-5.6-sol`，候选槽、ROI 和控制区是在看图后选择；报告自己也明确说明量化属于 post-hoc 描述，不能计算 precision 或 recall。固定 warp 和固定 ROI 的复现，只证明定量脚本可重复，不证明端到端自动检测有效。

因此，准确率提升目前属于“有机制依据的假设”，不是事实。

### 5.2 效率：全量调用一定更慢，选择性调用可能使总体性价比更好

每张图全量运行 SWE‑Vision 会新增：

- 至少一次模型决定工具的往返；
- 一次或多次 Python cell 执行；
- 工具结果图片的编码和再次视觉推理；
- Docker/Jupyter 启动或维护成本；
- 轨迹写盘。

所以相对当前“证据预计算后一次性发送”，单图时延不会更低。

更合理的直接调用方式是选择性复核：

- 高置信正常、高置信强物理证据样本沿用当前路径；
- 仅对调查与反证冲突、弱证据组合、OCR 与槽位不一致、局部质量不足但整牌仍可判别的样本调用 `VLMToolCallAgent.run()`；
- 每个疑难样本最多 1–2 次 `execute_code`，随后强制 `finish`；
- 同一台 GPU 只运行一个 Qwen3.5-2B 服务，普通判断和工具智能体共用同一个模型，不增加第二个模型的训练维护成本。

这样不会提高疑难样本的单图速度，但可能以较少的额外计算换取整体召回或槽位精度提升。

## 六、建议的信息流（仅设计，不实施）

```text
整车图
  -> 现有车牌定位与透视矫正
  -> 现有本地物理证据与三张标准视图
  -> Qwen3.5-2B 常规结构化判断
  -> 确定性后处理
       ├─ 高置信结果：直接输出
       └─ 疑难触发：直接调用 VLMToolCallAgent.run()
             -> execute_code（最多 1–2 次）
             -> 原图/槽位自适应裁剪与定量图
             -> finish(JSON)
             -> 现有槽位白名单、字符映射、串扰抑制和物理门控
             -> 最终输出
```

`execute_code` 应允许模型使用现成输入图和法证库，但最终裁决仍由当前项目的确定性后处理约束。模型生成的代码不能直接改项目文件、标签或阈值；容器只应写本次样本工作目录。

## 七、建议的验证设计

在任何生产改造前，先完成以下三组配对实验。所有组使用同一批图、同一车牌定位结果、同一随机顺序：

1. A 组：当前固定证据 + Qwen3.5-2B，不启用 SWE‑Vision。
2. B 组：所有样本都直接调用 `VLMToolCallAgent.run()`。
3. C 组：A 组为主，仅疑难样本调用 `VLMToolCallAgent.run()`。

重点比较 A 与 C；B 主要用于测量全量智能体的上限和代价。

建议先用 20 张做接口与失败模式试验，再在独立 70 张或新标注集上锁定评测。前 30 张如果已经参与提示词和规则调整，应作为开发集，不再单独宣称无偏测试成绩。

建议的最低准入要求：

- JSON 合法率 100%；
- Python 工具执行成功率至少 95%；
- 不增加 `unassessable` 滥用；
- C 组整牌 recall 或槽位 recall 有实际提升，同时 precision 不出现不可接受下降；
- 报告 Wilson 置信区间，样本不足时不把个位数差异解释为确定提升；
- 同时报告 P50/P95 时延和疑难触发率，不能只报平均速度；
- 单独检查蓝牌/绿牌、贴整字符/加笔画/消笔画、模糊/反光/倾斜等分层结果。

## 八、最终建议

1. 把 `Qwen/Qwen3.5-2B` 作为当前唯一主选，先用 BF16 建立可复现实测基线；MiniCPM-V 4.6 保留为速度备选，不同时维护两套生产模型。
2. 可以并且值得直接调用 SWE‑Vision 现有代码，但应调用完整的 `VLMToolCallAgent.run()`，只服务于疑难样本；单独调用 `JupyterNotebookKernel.execute()` 更适合固定脚本，无法获得自主分析能力。
3. 不把外部六图试验视为准确率证据；它只证明强模型配合代码工具可以形成可解释的探索性分析。
4. 在本项目 100 张标注集完成 A/C 配对测试前，不宣称 SWE‑Vision 提高了准确率；在测出收益前也不投入训练。
5. 如果 Qwen3.5-2B 的固定路径已经满足目标，优先保持简单；只有当错误集中在“固定面板没有覆盖、需要自适应放大或定量”的样本上，直接调用 `execute_code` 才具有明确工程价值。

## 九、资料来源与研究披露

本意见优先使用模型官方仓库、官方模型卡、官方部署文档和技术报告。模型方自报指标可能受提示模板、分辨率、采样参数和评测实现影响，跨模型数字不保证完全同口径。SWE‑Vision 部分来自本地源代码静态审查和项目已有试验报告，没有实际启动 Docker、下载模型或运行新的准确率实验。本意见由 AI 辅助检索与整理，所有生产结论仍需由本项目标注集复核。
